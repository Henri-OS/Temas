# Temas windows 10 usado no xfce

mkdir -p ~/.icons ~/.themes && cd ~ && wget -O windowsxp.zip https://github.com/Henri-OS/Temas/releases/download/Temas/windows10.zip && unzip -qo windows10.zip -d ~ && rm windows10.zip && echo "✅ Instalação concluída."

# Tema windows XP usado no xfce

mkdir -p ~/.icons ~/.themes && cd ~ && wget -O windowsxp.zip https://github.com/Henri-OS/Temas/releases/download/Temas/windowsxp.zip && unzip -qo windowsxp.zip -d ~ && rm windowsxp.zip && echo "✅ Instalação concluída."
